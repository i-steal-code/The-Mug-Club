# Supabase: Project Settings → Database → Connection string (URI)
# Render: set DATABASE_URL on the Web Service (do not commit real secrets).
#
# Direct host db.<project>.supabase.co:5432 — the app prefers IPv4 automatically
# (Render often cannot use IPv6 to Supabase).
# Alternative: use the "Connection pooling" / Session pooler URI if you prefer.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# Flask session signing (set a long random string in production)
SECRET_KEY=_____________________
